"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
export function AdminLoginForm(){const router=useRouter();const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password")})});const body=await response.json();if(!response.ok){setError(body.error||"Access denied.");setBusy(false);return}router.replace("/skie-control");router.refresh()}return <form aria-busy={busy} aria-describedby={error?"admin-login-error":undefined} className="admin-login-form" onSubmit={submit}><label>Admin email<input type="email" name="email" autoComplete="username" required placeholder="admin@skieevents.com"/></label><label>Admin password<input type="password" name="password" autoComplete="current-password" required minLength={8}/></label><button className="button button-primary" disabled={busy} type="submit">{busy?"Checking...":"Enter control"}<span aria-hidden="true">↗</span></button>{error&&<p className="form-message is-error" id="admin-login-error" role="alert">{error}</p>}</form>}
